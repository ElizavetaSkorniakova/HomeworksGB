import math

# Задача 2: Найдите сумму цифр трехзначного числа.
num = input('Введите трехзначное число:')
sum = 0
for i in range(0,3) :
    sum = sum + int(num[i])
print ( "Сумма всех цифр трехзначного числа =", sum)


# Задача 4: Петя, Катя и Сережа делают из бумаги журавликов. Вместе они сделали S журавликов. Сколько журавликов 
# сделал каждый ребенок, если известно, что Петя и Сережа сделали одинаковое количество журавликов, а Катя сделала в 
# два раза больше журавликов, чем Петя и Сережа вместе?
S = int ( input ('Введите количество журавликов, которое сделали дети:') )
print ( "Петя сделал", int ( math.floor (S/5) ), "журавлика/журавликов" )
print ( "Сережа сделал", int ( math.floor (S/5) ), "журавлика/журавликов" )
print ( "Катя сделала ", int (S-2*( math.floor (S/5) ) ), "журавлика/журавликов" )

# Задача 6: Вы пользуетесь общественным транспортом? Вероятно, вы расплачивались за проезд и получали билет с номером. 
# Счастливым билетом называют такой билет с шестизначным номером, где сумма первых трех цифр равна сумме последних трех. 
# Т.е. билет с номером 385916 – счастливый, т.к. 3+8+5=9+1+6. Вам требуется написать программу, которая проверяет счастливость билета
b = input('Введите шестизначный номер билета:')
sum1 = 0
sum2 = 0
for i in range(0,3) :
    sum1 = sum1 + int(b[i])
for i in range(3,6) :
    sum2 = sum2 + int(b[i])
if sum1 == sum2:
    print ( "Билет", b, "является счастливым")
else:
    print ( "Билет", b, "не счастливый")

# Задача 8: Требуется определить, можно ли от шоколадки размером n × m долек отломить k долек, если разрешается сделать один разлом по 
# прямой между дольками (то есть разломить шоколадку на два прямоугольника).
n = input ( "Введите количество долек по ширине шоколадки: " )
m = input ( "Введите количество долек по длине шоколадки: " )
k = input ( "Введите количество долек, которое хотите отломить от шоколадки: " )
if k == n or k == m:
    print ( k, "дольки/долек могут быть отломлены от шоколадки" )
else:
    print ( k, "дольки/долек нельзя отломить от шоколадки" )
